# cmd.conf — Agent 命令配置
# 格式：<基础命令> | <workspace 参数> | <继续对话参数> | <提示词参数>
# 每行一个 Agent，# 开头为注释
# 提示词参数会紧跟在问题文本前面，如 -p "问题内容"
# 示例：kode | -w | -c | -p
# 示例：kimi | -w | -C | -p

kode | | -c | -p
kimi | -w /MyWork/teamKanbanAgent | -C | -p
